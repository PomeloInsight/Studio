import React, { Component } from 'react';

import css from './style.scss';

class EditorCore extends Component {
  render() {
    return <div className={css.editorCore}>editor core</div>;
  }
}

export default EditorCore;
